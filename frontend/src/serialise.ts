import { treeget, treeinsert, treemerge } from './helpers'


type Serialisable = string | number | boolean | null | {[key:string]:Serialisable} | Serialisable[]


const serial = (x:Serialisable):string =>{

  

  return ''
}
