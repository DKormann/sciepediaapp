{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build:gh-pages": "BUILD_TARGET=gh-pages vite build --force",
    "build": "BUILD_TARGET=gh-pages vite build --force",
    "build:local": "tsc && vite build --force",
    "preview": "vite preview",
    "test": "jest",
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1",
    "typescript": "^4.5.4",
    "vite": "^3.0.7"
  }
}