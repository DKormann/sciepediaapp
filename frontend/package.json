{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build --force",
    "build:gh-pages": "BUILD_TARGET=gh-pages vite build --force",
    "preview": "vite preview",
    "test": "vite test"
  },
  "devDependencies": {
    "typescript": "^4.5.4",
    "vite": "^3.0.7"
  }
}